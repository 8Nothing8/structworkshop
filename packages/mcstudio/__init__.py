"""mcstudio —— structworkshop 本地浏览器工作台（模块库 / 标签 / 3D 预览 / 结构编辑）。

零新增依赖：HTTP 用标准库，三维渲染用 vendor 的 deepslate（浏览器端）。
    python -m mcstudio serve --open
"""
from __future__ import annotations

__version__ = "0.1.0"
