# structworkshop 环境变量示例
#
# 复制成 .env（已 gitignore）或直接 export 到 shell。
# 全部都是**可选**的：不设任何变量，`python -m mccore.compose math-cube` 也能跑。
# 旧名 MCFORGE_* 仍然认（改名兼容），但新名优先 —— 新代码请只用 STRUCTWORKSHOP_*。

# ---- 仓库位置 ----
# 只有「从仓库外面」调用时才需要；仓库内调用会自己往上找 pyproject.toml。
# STRUCTWORKSHOP_ROOT=/path/to/structworkshop

# ---- 项目输出位置（一般由 `mccore.compose --project` 自动设置，别手写）----
# STRUCTWORKSHOP_PROJECT_DIR=/path/to/compositions/<id>/projects/<name>

# ---- 大结构的安全阀（防手滑把 100 万方块的 .schem 读进内存）----
# 单位：MB / 体素格数 / 非空气方块数；WARN_ 是「只警告」的阈值。
# 环境变量固定住的值会在工作台设置页置灰并标注。
# STRUCTWORKSHOP_MAX_STRUCTURE_MB=12.5
# STRUCTWORKSHOP_MAX_STRUCTURE_CELLS=20000000
# STRUCTWORKSHOP_MAX_STRUCTURE_BLOCKS=4000000
# STRUCTWORKSHOP_WARN_STRUCTURE_CELLS=5000000

# ---- 视觉评审（mcqa.vision_review / tools/vreview.py / mccore.stage_build）----
# 三种给 key 的方式任选其一（优先级从高到低）：
STRUCTWORKSHOP_VISION_API_KEY=
# STRUCTWORKSHOP_AUTH_FILE=~/.config/structworkshop/auth.json
#   默认位置（二选一）：~/.config/structworkshop/auth.json 或 ~/.structworkshop/auth.json
#   内容形如：{"deepseek": {"key": "sk-..."}}
#
# 换供应商 / 自建反代（任何 OpenAI 兼容的 /chat/completions 都行）：
# STRUCTWORKSHOP_VISION_API=https://your-endpoint/v1/chat/completions
# STRUCTWORKSHOP_VISION_MODEL=your-vision-model

# ---- 字体（中文铭碑 glyph 工具、画廊/图表标签）----
# 指定一个能画中文的 TTF/TTC 绝对路径，比自动探测稳。
# STRUCTWORKSHOP_FONT=/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc

# ---- 新资产包的默认署名（pack create / scan 写进 pack.json）----
# 不设的话会去读 `git config user.name`，再不行写 "unknown"。
# STRUCTWORKSHOP_AUTHOR=Your Name
